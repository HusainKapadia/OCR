prwaitbar off



data = prnist([0:9], [1:40:1000]);
show(data);


features = my_rep(data);


%bbox = 

%normalize
%normalized = ...; 

net = bpxnc(features, 12);

%TODO: Get 2D features
scatterd(features, 'legend');

plotc(net);



untrainedNet =  bpxnc([], 12);
nist_eval('my_rep', net)